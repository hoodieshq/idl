{
    "name": "idl-example-browser-esbuild",
    "version": "0.0.0",
    "private": true,
    "description": "Minimal esbuild browser example for @solana/idl.",
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "serve": "serve dist -l 8771"
    },
    "dependencies": {
        "@solana/idl": "workspace:*",
        "@solana/kit": "^6.8.0"
    },
    "devDependencies": {
        "esbuild": "^0.24.0",
        "serve": "^14.2.4"
    }
}
